services:
  - type: web
    name: whisper-asr
    plan: free
    envVars:
      - key: WHISPER_CACHE_DIR
        value: /tmp/whisper-cache
      - key: HF_HOME
        value: /tmp/huggingface
      - key: HF_HUB_CACHE
        value: /tmp/huggingface/hub
